import { combineReducers } from "redux";
import {GetMetrics} from '../reducers/metrics'
import {GetTime} from '../reducers/time'

export const reducer = combineReducers({
  
GetMetrics,
GetTime


});
